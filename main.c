/* p5_7 p5_7_error */ 

// a)  function shouldn't be nested. write them individually. 

// b) not value returned. add return result ; 

// c) float a is defined in the argument and a semicolon after parenthesis. remove declaration of float a from function block . 

// d) assigment to n must be done in else block, i.e. n += sum( n - 1 ) ;  

// e) remove return statement. since return type is void. 
