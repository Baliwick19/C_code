/* p3_43 errors  */ 


/*
	=> printf("%d", --(x * y)); ; 
	
	increment or decremrnt operator cannot be used on expressions. 
	
	->  int z = x * y ; 
		printf("%d", --(z)) ;

	therfore, we must create a another variable to store value of x * y. 
	
*/


